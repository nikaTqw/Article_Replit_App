## Packages
recharts | Data visualization for the interactive price/quality scatter plot
framer-motion | Smooth section reveals and page animations

## Notes
- `lucide-react` is used for all dynamic icons.
- Expecting Postgres `numeric` values (`pricePer1k`, `qualityScore`) as strings in the API responses, parsing them to floats in the frontend.
